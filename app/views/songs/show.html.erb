<p id="notice"><%= notice %></p>

<h1><%= @song.title %></h1>

<h2><%= @song.artist %></h2>

<%= render 'song', song: @song_pro, chords: @chords %>

<%= link_to 'Edit', edit_song_path(@song) %> |
<%= link_to 'Back', songs_path %>
